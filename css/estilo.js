import { StyleSheet } from 'react-native';

const mascara = StyleSheet.create({
  fundo: {
    flex: 1,
    backgroundColor: '#f00',
  },
  titulo: {
    color: '#0ff',
    textAlign: 'center',
    padding: '8px 12px',
  },
});

export default mascara;
